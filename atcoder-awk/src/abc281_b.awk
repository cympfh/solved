#!/usr/bin/awk -f

# 入力から文字列 S を取得し、そのフォーマットが正しいかどうかを判定する
{
  # 入力から文字列 S を取得
  S = $0

  # 文字列 S が英大文字1文字 + 6桁の整数 (0から始まらない) + 英大文字1文字のパターンと一致するかどうかを判定する
  if (match(S, /^[A-Z][1-9][0-9]{5}[A-Z]$/)) {
    # 条件を満たす場合は Yes を出力
    print "Yes"
  } else {
    # 条件を満たさない場合は No を出力
    print "No"
  }
}
